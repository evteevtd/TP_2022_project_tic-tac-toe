# project(players)

# add_library(human_player human_player.cpp)
# add_library(random_ai_player random_ai_player.cpp)

# add_library(players INTERFACE)
# target_link_libraries(players INTERFACE human_player random_ai_player)
